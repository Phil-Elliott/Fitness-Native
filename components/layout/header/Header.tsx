import { View, Text } from "react-native";

const Header = () => {
  return (
    <View className="flex justify-center items-center bg-gray-100 h-16 border-t border-gray-200">
      <Text className="text-xl">Header</Text>
    </View>
  );
};

export default Header;
